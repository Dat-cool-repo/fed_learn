# Configs

This folder separates shared project defaults from machine-specific paths.

- `experiments/`: shared research settings such as rounds, clients, and PEFT methods
- `models/`: model-specific defaults
- `local.example.toml`: template for each teammate to copy into `local.toml`

Commit shared configs. Do not commit `local.toml`.

